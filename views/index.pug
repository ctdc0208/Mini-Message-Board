extends layout

block content
  h1= title
  p Welcome to #{title}

  a(href='/new' class='button') New Message

  ul
    each val in messages
     li 
       p #{val.text}
       p #{val.user}
       p #{val.added}